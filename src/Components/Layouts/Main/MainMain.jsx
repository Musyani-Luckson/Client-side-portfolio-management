import { NavLink, Outlet } from "react-router-dom";
function MainMain() {
  return (
    <main className="mainMain layouts" id="mainMain">
      <Outlet />
    </main>
  )
}

export default MainMain
